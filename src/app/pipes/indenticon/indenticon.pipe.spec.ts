import { IndenticonPipe } from './indenticon.pipe';

describe('IndenticonPipe', () => {
  it('create an instance', () => {
    const pipe = new IndenticonPipe();
    expect(pipe).toBeTruthy();
  });
});
